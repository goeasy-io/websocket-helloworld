version: '1.0'
name: CD-latest
displayName: CD-latest
triggers:
  trigger: manual
  push:
    branches:
      prefix:
        - 跑不通_master
variables:
  latest_version: ''
  global:
    - GIT_HUB_USER
    - GIT_HUB_TOKEN
stages:
  - name: stage-b9750053
    displayName: deploy
    strategy: naturally
    trigger: auto
    executor: []
    steps:
      - step: build@nodejs
        name: deploy
        displayName: deploy
        nodeVersion: 14.16.0
        commands:
          - '# 执行部署脚本'
          - bash deploy_latest.sh ${latest_version} ${GIT_HUB_USER} ${GIT_HUB_TOKEN}
        caches:
          - ~/.npm
          - ~/.yarn
        notify: []
        strategy:
          retry: '0'
